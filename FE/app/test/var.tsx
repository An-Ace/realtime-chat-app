const node_production = true;
export const socketUrl = node_production ? 'https://socket.itemstore.id': 'http://localhost:4000'
export const apiUrl = node_production ? 'https://api.itemstore.id': 'http://localhost:5000'